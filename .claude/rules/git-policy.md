# Git policy (this repo)

1. **No pull requests.** Commit straight to `master` — no feature branches, no PR
   workflow.
2. **Never commit or push until explicitly asked.** Leave finished work in the
   working tree and say it's ready; wait for the user to ask for the commit.
